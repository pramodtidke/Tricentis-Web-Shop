apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shopwave-notification-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/pramodtidke/Tricentis-Web-Shop.git
    targetRevision: main
    path: helm/shopwave-microservice
    helm:
      valueFiles:
        - values-notification-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: shopwave
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
